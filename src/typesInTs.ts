let name = "Ashish"
//name = 45
let cup = Math.random() > 0.5 ? 10 : 6
console.log(name);   //typeinferencing :- when ts understand default types by itself without mention types manually.which is you see in upper two lines


let Class:number = 10
let village:string = "Ambala" //This is called Type Annotations.
 //mentioning data types manually.